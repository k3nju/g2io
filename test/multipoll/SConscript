#! /usr/bin/python
# -*- coding:utf-8 -*-

import os;
Import( "LIBPATH" );
Import( "LIBS" );
Import( "build_mock" );

libs = ["dispatcher"] + LIBS[:];
libpath = ["../../build/multipoll/"] + LIBPATH[:];

build_mock( libpath, libs );
