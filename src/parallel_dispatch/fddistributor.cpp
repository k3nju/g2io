#include "fddistributor.h"

namespace g2io
	{
	/*
	//-----------------------------------------------------------------------------------------//
	void SetWorkers( Worker *workers, size_t count )
		{
		workers_ = workers;
		count_ = count;
		}
	
	//-----------------------------------------------------------------------------------------//
	void FDDistributor::Register( int fd, int events, epoll_data_t *data )
		{
		size_t i = __sync_fetch_and_add( &seq_, 1 ) % count_;
		( workers + i )->Register( fd, events, data );
		}
	*/
	}
